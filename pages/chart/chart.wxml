<cu-custom bgColor="bg-gradual-green" isBack="{{true}}">
  <view slot="backText">返回</view>
  <view slot="content">图表分析</view>
</cu-custom>
<view  class="container flex flex-wrap bg-gray ">
  <block wx:for="{{ecs}}" wx:key="index"> 
    <ec-canvas id="ec{{index}}" ec="{{ item.ec }}" 
       style="width:{{item.width}};height:{{item.height}}"></ec-canvas>
  </block>  
</view>