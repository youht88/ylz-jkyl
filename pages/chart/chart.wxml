<cu-custom bgColor="bg-gradual-green" isBack="{{true}}">
  <view slot="backText">返回</view>
  <view slot="content">图表分析</view>
</cu-custom>
<view  class="container padding" style="width:100%;height:400px">
  <block wx:for="{{options}}" wx:key="index">
    <ec-canvas id="echart{{index}}" ec="{{ec}}" style="width:{{item.width}};height:{{item.height}}">
    </ec-canvas>
  </block>
</view>
